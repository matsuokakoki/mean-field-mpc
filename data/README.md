# Data

The pipeline downloads the official Microsoft Azure Functions Invocation Trace
for two weeks starting 2021-01-31. Raw and processed third-party data are ignored
by git. Run `python -m mfcontrol data download` and `data prepare` inside Docker.

